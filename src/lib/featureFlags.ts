/**
 * Hides "Resources" content types:
 * - /blogs, /newsletters, /case-studies and their /[id] routes (404)
 * - related navigation, homepage sections, search results, sitemap entries
 */
export const HIDE_RESOURCES = true;

